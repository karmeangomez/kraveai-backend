{
  "name": "kraveai-backend",
  "version": "1.0.0",
  "description": "Backend for KraveAI Instagram Scraper",
  "main": "index.js",  // ¡Asegúrate que dice index.js!
  "scripts": {
    "start": "node index.js",  // ¡Asegúrate que dice index.js!
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^22.6.0"
  },
  "puppeteer": {
    "executablePath": "/usr/bin/chromium",
    "args": [
      "--no-sandbox",
      "--disable-setuid-sandbox",
      "--disable-dev-shm-usage",
      "--disable-accelerated-2d-canvas",
      "--no-first-run",
      "--no-zygote",
      "--single-process",
      "--disable-gpu"
    ]
  }
}
