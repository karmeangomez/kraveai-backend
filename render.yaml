# render.yaml
buildCommand: |
  sudo apt-get update
  sudo apt-get install -y chromium-browser
  npm install

envVars:
  - key: CHROMIUM_PATH
    value: /usr/bin/chromium-browser  # Ruta estándar en Ubuntu
